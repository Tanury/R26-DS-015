import { AppShell } from "@/components/app-shell";
import { PageHeader } from "@/components/page-header";
import { Card, CardContent } from "@/components/ui/card";
const questions=[["Is this a diagnostic system?","No. It is an experimental research screening interface. Results require professional review and cannot establish a diagnosis."],["Where is assessment history stored?","History is stored only in this browser’s local storage. Clearing browser data removes it."],["How is voice processed?","The browser sends audio to FastAPI. The server calls Gemini with its server-side key, validates 14 structured estimates, and passes them to the classifier."],["Which files are supported?","Voice assessment accepts WAV, MP3, M4A, OGG, and WebM files up to 18 MB. EEG assessment accepts EEGLAB .set recordings with their .fdt companion, up to 120 MB."],["What does the biomedical assessment need?","Fourteen quantitative marker values that were already measured elsewhere. No audio is recorded or uploaded on that page — the classifier scores the measurement sheet directly."]];
export default function SupportPage(){return <AppShell><PageHeader title="Help and Support" description="Key information about research use, privacy, and supported assessment workflows."/><div className="grid gap-4 lg:grid-cols-2">{questions.map(([title,copy])=><Card key={title}><CardContent className="p-6"><h2 className="text-lg font-bold">{title}</h2><p className="mt-3 text-sm leading-7 text-slate-600">{copy}</p></CardContent></Card>)}</div></AppShell>}
